# {{ namespace }}.{{ role_name }}

{{ role_description or "An Ansible role." }}

## Requirements

- ansible-core >= {{ min_ansible_version }}
- [uv](https://docs.astral.sh/uv/) for local development
{% if include_vagrant %}
- libvirt + Vagrant + KVM for the VM test scenario
{% endif %}

## Role variables

See [`defaults/main.yml`](defaults/main.yml).

## Example playbook

```yaml
- hosts: all
  become: true
  roles:
    - role: {{ namespace }}.{{ role_name }}
```

## Development & testing

Managed with [copier](https://copier.readthedocs.io/) + [uv](https://docs.astral.sh/uv/):

```bash
uv sync          # install dev toolchain
make lint        # yamllint + ansible-lint
{% if include_docker %}make test        # molecule test — docker scenario (fast)
{% endif %}{% if include_vagrant %}make test-vm     # molecule test — vagrant/libvirt scenario (needs KVM)
{% endif %}```

Pull future template improvements:

```bash
copier update --trust
```

## License

{{ license if license != 'none' else 'Proprietary' }}
