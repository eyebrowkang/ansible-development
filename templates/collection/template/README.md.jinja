# {{ namespace }}.{{ collection_name }}

{{ collection_description or "An Ansible collection." }}

## Included content

- `roles/example` — starter role; replace with your real roles.

## Requirements

- ansible-core >= {{ min_ansible_version }}
- [uv](https://docs.astral.sh/uv/) for local development
{% if include_docker %}- Docker (for the molecule docker scenario; `ansible-test sanity` runs in a `--venv`)
{% endif %}{% if include_vagrant %}- libvirt + Vagrant + KVM (for the vagrant molecule scenario)
{% endif %}

## Installing

```bash
ansible-galaxy collection install {{ namespace }}.{{ collection_name }}
```

## Development & testing

Managed with [copier](https://copier.readthedocs.io/) + [uv](https://docs.astral.sh/uv/):

```bash
uv sync          # install dev toolchain
make lint        # yamllint + ansible-lint
make sanity      # ansible-test sanity (--venv)
{% if include_docker %}make test        # molecule test for the example role (docker)
{% endif %}{% if include_vagrant %}make test-vm     # molecule test for the example role (vagrant; needs KVM)
{% endif %}```

> `ansible-test` must run from an `ansible_collections/{{ namespace }}/{{ collection_name }}/`
> path. Developing under the meta-repo's `collections/` workspace satisfies this; CI checks
> the repo out under that path (see the generated CI workflow).

Pull future template improvements:

```bash
copier update --trust
```

## License

{{ license if license != 'none' else 'Proprietary' }}
