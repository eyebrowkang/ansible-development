---
# tasks for the {{ namespace }}.{{ collection_name }}.example role
- name: Replace this placeholder with the role's real tasks
  ansible.builtin.debug:
    msg: "{{ namespace }}.{{ collection_name }}.example applied"
