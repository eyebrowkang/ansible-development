---
# Default variables for the example role. Users override these in inventory/playbooks.
#
# Example (replace with your role's real defaults). Prefix every role variable with the
# role name so it can't collide across roles (ansible-lint var-naming[no-role-prefix]).
example_marker_path: "/etc/{{ namespace }}.{{ collection_name }}.example.applied"
